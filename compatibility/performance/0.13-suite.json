{
  "schema_version": 1,
  "id": "oxiland-redland-0.13-v1",
  "status": "draft",
  "baseline": "Redland librdf 1.0.17",
  "surface": "public C API",
  "thresholds": {
    "throughput_oxiland_over_redland_min": 1.05,
    "latency_oxiland_over_redland_max": 0.95,
    "throughput_ci_lower_min": 1.0,
    "latency_ci_upper_max": 1.0,
    "confidence": 0.95,
    "bootstrap_rounds": 10000,
    "minimum_samples": 100,
    "independent_host_runs": 3
  },
  "protocol": {
    "require_production_compile": true,
    "optimization": "Cargo --release with target-cpu=native for Oxiland; matched -O3 -march=native for Redland and C wrappers",
    "order": "paired per-sample AB/BA after per-process warm-up",
    "minimum_sample_duration_ms": 10,
    "resource_samples": 5,
    "resource_aggregation": "median of isolated child-process peak RSS measurements",
    "validation": "every calibrated repetition validates the completed RDF result",
    "isolation": "same host, storage medium, allocator policy, thread limit, and input bytes",
    "claim": "every host/case cell must pass in three independent runs"
  },
  "cases": [
    {"id": "P-MUT-1K", "operation": "insert 1000 triples", "kind": "throughput", "size": "small", "required": true},
    {"id": "P-MUT-10K", "operation": "insert 10000 triples", "kind": "throughput", "size": "large", "required": true},
    {"id": "P-SCAN-10K", "operation": "full model scan over 10000 triples", "kind": "throughput", "size": "large", "required": true},
    {"id": "P-PARSE-TTL-1K", "operation": "parse Turtle with 1000 triples", "kind": "throughput", "size": "small", "required": true},
    {"id": "P-PARSE-TTL-10K", "operation": "parse Turtle with 10000 triples", "kind": "throughput", "size": "large", "required": true},
    {"id": "P-SER-NQ-10K", "operation": "serialize 10000 triples as N-Quads", "kind": "throughput", "size": "large", "required": true},
    {"id": "P-ASK-10K", "operation": "ASK over 10000 triples", "kind": "latency", "size": "large", "required": true},
    {"id": "P-SELECT-10K", "operation": "SELECT and consume up to 1000 bindings over 10000 triples", "kind": "throughput", "size": "large", "required": true},
    {"id": "P-GRAPH-10K", "operation": "CONSTRUCT and consume up to 1000 triples", "kind": "throughput", "size": "large", "required": true},
    {"id": "P-CALL-100K", "operation": "100000 model size calls", "kind": "throughput", "size": "overhead", "required": true}
  ],
  "resource_budgets": [
    {"id": "R-RSS-PARSE", "unit": "ratio", "maximum": 1.25},
    {"id": "R-RSS-QUERY", "unit": "ratio", "maximum": 1.25}
  ],
  "milestone": "0.13",
  "notes": "Draft strict faster-than-Redland gate. Freeze only after the paired harness and three-host baselines are reviewed."
}

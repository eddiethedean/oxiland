# 0.7.0 release checklist

Status: ready for tagging after CI green on the release tip

## Preflight

- [x] ADR-017 accepted; milestone + design docs present
- [x] `python/` package builds (`maturin develop`)
- [x] Pytest green locally
- [x] Pyright green locally
- [x] Examples run
- [x] CI jobs added: `Python`, `Python wheels`
- [x] `docs/reports/0.7.md`, user guide, parity/roadmap updates prepared
- [ ] CI green on tip (Check + Python + Python wheels + Fjall + MSRV + conformance)
- [ ] GitHub tag `v0.7.0` (Release workflow publishes crates.io + PyPI)

## Commands

```console
cargo test
cargo clippy --all-targets -- -D warnings
cd python && maturin develop && pytest && pyright
```

Tagging triggers the Release workflow (`.github/workflows/release.yml`):
crates.io via `CARGO_REGISTRY_TOKEN`, PyPI **wheels only** via `PYPI_API_TOKEN`
(`maturin publish --no-sdist` / `MATURIN_PYPI_TOKEN`). Source distributions are
not published in 0.7.0 (path dependency on the Rust crate).

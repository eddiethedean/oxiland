# Milestone 0.11 parity report

Status: complete

Milestone: [`0.11`](../milestones/0.11.md)

## Verdict

`scripts/check-0.11-release.py` passes on the six-cell native differential
bundle (90 fixture results, 1,532 obligations) bound to revision
`37d480ffd493e1e6549254cc587322e16afb7c4a`, with non-synthetic performance
evidence on each performance profile and soak/fuzz records at the same
revision.

## Delivered packages

| Package | State | Evidence |
|---|---|---|
| WP-11-01 baseline + obligations | done | `compatibility/baseline/0.11-baseline-manifest.json`, `0.11-obligations.json`, `redland-1.0.17-oxiland-0.11.json` |
| WP-11-02 two-sided harness | done | `compatibility/harness/two_sided.py` + `fixtures/0.11/` |
| WP-11-03 fail-closed checker | done | `scripts/check-0.11-release.py`, `scripts/derive-0.11-parity.py` |
| WP-11-04 safe workflows | done | `tests/obligations_0_11.rs` + harness differentials |
| WP-11-05 C source corpus | done | `compatibility/downstream/corpus/` (`-Werror`, Redland + Oxiland) |
| WP-11-06 binary ABI packaging | done | `scripts/package-librdf-compat.sh`, `compatibility/downstream/abi/` |
| WP-11-07/08 soft stubs | done | iostream handles, factory callbacks, parser features, logging locator |
| WP-11-09 safety / fuzz | done | `compatibility/qualification/0.11-fuzz.json` (≥3600s executed per target) |
| WP-11-10 cross-platform raw | done | `compatibility/qualification/raw/` for Linux, macOS, Windows × both profiles |
| WP-11-11 perf + packaging | done | native `performance/*.json`; package-gate corpus/ABI in CI |
| WP-11-12 soak + release | done | `0.11-soak.json`; checker green; docs updated for 0.11.0 |

## Hard gates

| Gate | State | Notes |
|---|---|---|
| Complete baseline denominator | closed | Checksummed headers + export dump + obligations |
| Native two-sided differential harness | closed | Harness emits raw observations; rejects missing Redland |
| Safe workflow parity | closed | Obligation-linked Rust tests + fixture coverage |
| C source compatibility | closed | Frozen corpus passes Redland and Oxiland `-Werror` builds |
| Binary ABI interchange | closed | librdf-compat packaging + ABI-swap smoke |
| Full observable behavior | closed | All six profiles differential_passed with clean worktrees |
| Cross-platform provenance | closed | `.github/workflows/qualify-0.11.yml` run `30733139478` |
| Candidate safety and conformance | closed | Fuzz ≥3600s executed; soak revision-bound |
| Performance and packaging retention | closed | Native host evidence; CI package gate |
| Independent reproduction and soak | closed | Checker passes; soak abi_resets=0 |

## Completion rule

This report is `complete` because `scripts/check-0.11-release.py` derives a
passing result from linked raw evidence for every frozen target and profile.
Counts from an inventory, an export allowlist, or a manually asserted profile
result cannot close a gate.

# Oxiland 0.12.0 release checklist

Status: tip preflight

Tag target: `v0.12.0`

## Release scope

0.12.0 closes the performance-optimization milestone: frozen competitive-parity
gate ([ADR-028](../DECISIONS.md#adr-028--012-competitive-parity-performance-gate)),
production-compile evidence on Linux/macOS/Windows `release-default`, RSS
budgets, and retained 0.11 parity on the candidate ancestry. See the
[0.12 report](0.12.md).

## Preflight

- [ ] Tip CI green on `main` before tagging
- [x] Package and lockfile versions match `v0.12.0`
  (`python scripts/check-release-version.py v0.12.0`)
- [x] `python scripts/check-0.11-release.py` (parity retention)
- [x] `python scripts/check-0.12-release.py`
  (3 profiles, 30 cases, 6 resource checks on revision
  `34c0976fdca813aff7e701a1c2e617ed0832b99f`)
- [ ] Rust format, clippy, workspace tests, MSRV, C ABI allowlist, and docs
  (tip CI)
- [ ] `cargo publish -p oxiland --locked --dry-run`

## Publish

1. Confirm checkers green on the tag commit.
2. Tag `v0.12.0` and push the tag.
3. Confirm the release workflow publishes crates/wheels as configured.

## Notes

Do not market a blanket “faster than Redland” claim from this release; cite
per-case ratios from `compatibility/qualification/performance/0.12/`.
